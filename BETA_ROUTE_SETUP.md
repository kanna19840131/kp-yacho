# KP野帳 β 担当路線セットアップ

## 目的

有料βで顧客ごとの路線データを公開リポジトリへ直接埋め込まず、担当路線パッケージJSONとして端末へ読み込ませる。

`beta.html` 自体は共通で使い、顧客ごとの差分は `kp-yacho-route-package` だけにする。

## 標準の受注入力

顧客にKMLやGISデータの準備を標準要求しない。

最初に確認するのは次の3点。

1. 対象路線名 / 路線番号
2. 実際に使用するKP範囲
3. 現場で確実に確認できるKP地点・契約図書・地点標など

提供側で利用条件を確認できる道路Geometryを準備できる場合は、提供側で線形候補を作る。

利用可能なGeometryを準備できない特殊路線、独自線形、構内道路、非公開区間等に限り、顧客が利用権を持つKML等を追加依頼する。

国道234号の内部納品リハーサルでは、顧客KMLなしでN13道路中心線から約69.65kmの路線Geometry候補を自動抽出できた。ただしGeometryとKP Referenceは別物として扱い、現場KPの最終確認を省略しない。詳細は `R234_DELIVERY_REHEARSAL.md`。

## 重要

このGitHubリポジトリは公開です。

顧客から受領したKML、契約図書、非公開の路線座標、現場固有データをこの公開リポジトリへcommitしないでください。

担当路線パッケージは、利用権・再利用条件・秘密保持条件を確認したデータだけで作成し、顧客へ個別に渡します。

## 路線パッケージ形式

`beta-route-package.example.json` を構成例として使う。

有料βは **1パッケージ = 1担当路線** とする。1路線の中に複数sectionがある構成は可。

必須項目:

- `type`: `kp-yacho-route-package`
- `schemaVersion`: `1`
- `packageId`: 顧客・路線ごとに重複しないID。英数字・`.`・`_`・`-` のみ、80文字以内
- `label`: 利用者へ表示する担当路線名
- `routes`: Route Engineが読める路線定義を1件だけ入れる
- `rights.confirmed`: 必ず `true`
- `rights.basis`: 顧客保有、許諾、利用条件確認済み公開データ等の利用権確認根拠

推奨項目:

- `metadata.fieldVerified`: 実路線で既知KP照合まで終わったか
- `metadata.sourceType`: `customer-kml`、`licensed`、`official-open-data` 等

利用権確認済み情報がないroute packageは、`beta.html` のruntime側でも読み込みを拒否する。

## GeometryとKP Referenceを分ける

道路中心線、KML、公開線形は「道路がどこを通っているか」を表すGeometryとして扱う。

Geometryの延長だけから現場KPを決めない。KPは別のReference/Anchorとして重ねる。

公開データを使う場合も、欠損・重複・版・局所的不整合があり得るため、一つのデータ源を絶対的な正解教師にしない。

参照点の自動採用には、必要に応じて次を照合する。

- 道路Geometryからの横距離
- 別参照系の同KPとの距離
- 前後KPから見た局所stationing整合
- KP順と線形上の進行方向

独立した複数信号が大きく矛盾する場合は自動採用せず、`verify` または `reject` とする。

## 既知KPアンカーの条件

有料βでは、原則として `polyline + anchors` または `sections[]` を使う。

- 各sectionに異なるKPのアンカーを2点以上持たせる
- 同一section内のKPは、線形に沿って単調増加または単調減少であること。途中反転は拒否する
- 同一section内で同じKPを異なる位置へ重複登録しない
- Geometryだけの幾何距離を現場KPとみなさない
- 使用予定KP範囲をアンカーで挟む
- アンカー範囲外は `extrapolated` としてβ画面側で自動KPを採用しない
- `kp-calibration-capture.html` 由来の点でGPS精度50m超、または `quality: poor` の点はBuilderでアンカー利用を拒否する
- 20m超〜50m以下の採取点は利用可能でも注意扱いとし、可能なら精度の良い地点で取り直す
- 登録路線から30m超は注意、200m超は対象路線外として自動KPを止める（初期値）

アンカーは「多ければ多いほど良い」と決め打ちしない。品質・配置・使用範囲を挟むことを優先する。

最初の実地確認では最低3地点を推奨し、少なくとも1地点をCalibrationに使わない答え合わせ用として残す。

## 納品までの流れ

1. 顧客から対象路線名 / 路線番号と、実際に使うKP範囲を確認する。
2. 提供側で利用条件を確認できる道路Geometryを準備できるか調査する。
3. 準備できない場合だけ、顧客が利用権を持つKML等を追加依頼する。
4. Geometry候補をQuality Gateに通し、対象路線として連続していることを確認する。
5. 現場・契約図書等で確実に分かるKPを最低3地点用意する。必要なら `kp-calibration-capture.html` でGPS座標を採取する。
6. 異常参照点をQuality Gateで除外・要確認化する。
7. 使用予定範囲が信頼できるアンカー内に収まることを確認する。
8. 少なくとも1地点を未使用の答え合わせとしてKP誤差を確認する。
9. 担当路線パッケージJSONを生成する。
10. Android端末で `beta.html` を開き、パッケージを読み込む。
11. 実路線でGPS→推定KP、手動KP、施工前登録、編集、保存、再読込、バックアップを一周する。
12. `BETA_RELEASE_CHECKLIST.md` を完了してから顧客へ渡す。

## 端末側

`beta.html` の「担当路線パッケージ読込」からJSONを一度読み込む。

路線データはブラウザのlocalStorageへ保存される。ページ再読込後も保持される。

ブラウザデータを削除した場合は再読込が必要なので、顧客には路線パッケージJSONも保管してもらう。

施工記録のバックアップJSONと路線パッケージJSONは別物。
