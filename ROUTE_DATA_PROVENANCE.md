# KP路線データの出自・検証ポリシー

## 目的
KP野帳では、`路線上に見える`ことと`現場で使うKPが正しい`ことを分けて扱う。

路線データごとに、少なくとも以下を保持する。

- データ提供者 / 取得元
- データ種別（実測 / 道路ネットワーク計算 / 旧変換ツール / 顧客KML / 現場実測）
- 作成・更新時期が分かるか
- 現旧道、上下線、補助番号等の識別
- 欠損 / 重複 / 不連続の監査結果
- 現地のKP・契約図書等で検証済みか

## 現行R230/R453データの内部系譜
GitHub上の現行`byRoute`は、GitHubへ最初から生成ロジック付きで入ったものではない。

初期のKP野帳ファイルには「前に動いたKPアプリのbyRouteデータを貼る」と記載されており、GitHub以前のデータを移植した構造になっている。

File Libraryで確認できた最古の具体データは、2026-06-05作成の`kp_230_453.csv`と、その直後の`kp_230_453_current.html`。このCSVのR230/R453の0.1km座標列が後続アプリへ引き継がれている。

したがって、現時点で確認できる内部系譜は次の通り。

`GitHub現行byRoute`
← `2026-06-10前後のKP野帳統合版`
← `kp_230_453_current.html / kp_230_453.csv（2026-06-05）`
← **生成元はGitHub内部からは確認できない**

## 外部公開データとの一致仮説
北海道向けに0.1kmピッチの国道KP KMLを公開しているHG企画は、北海道データについて「号線KP⇒経緯度変換ツール(Ver20121010)」から作成したと明記している。

公開ページにはR230・R453の0.1kmデータ導線があり、現行CSVが作られた2026-06-05と同日にページ更新記録もある。

このため、現行`kp_230_453.csv`がHG企画系データ、または同じ`号線KP⇒経緯度変換ツール(Ver20121010)`を元に生成された可能性は高い。

ただし、HG企画の公開Google Drive KMLをこの検証環境から取得できず、座標の完全一致比較はまだできていない。

**結論：強い出自仮説ではあるが、完全一致による確定はしていない。**

## 公式データにも種類がある
国土交通省 道路基準点案内システムでは、少なくとも次を区別する必要がある。

### 実測版
- 実際の地点標近傍に設置した道路基準点を公共測量したもの。
- 現地の1km地点標との対応確認に使う候補。

### 道路ネットワーク版
- DRM線形上で距離標を定義し、計算した座標。
- 100m地点を連続的に得やすい一方、物理的な地点標そのものの実測値ではない。

`official-route-adapter.js`で取得している100m CSVは後者として扱い、Route Packageにも`referenceClass: road-network-derived`、`fieldMarkerEquivalent: false`、`fieldVerified: false`を記録する。

## 実測1km / 道路ネットワーク100m / 現行legacyの比較
0〜10KPで比較した結果、道路上の位置関係は近いものの、同じKP値をどこに置くかには数十m級の差が残る。

### R230
- 実測1km → 同名ネットワーク地点: 平均約17.4m
- 実測1km地点 → 現行legacy KP目盛: 平均約24.2m相当
- 実測1km地点 → 現行legacy路線線形の横距離: 平均約13.1m

### R453
- 実測1km → 同名ネットワーク地点: 平均約24.2m
- 実測1km地点 → 現行legacy KP目盛: 平均約23.6m相当
- 実測1km地点 → 現行legacy路線線形の横距離: 平均約22.9m
- 3KP付近ではlegacy KP目盛との差が約89m相当の地点も確認した。

このため、現行legacyを新データの正解判定に使うことも、公開ネットワーク100mを現場KPの正解とみなすこともできない。

## 商品版の原則
Route Packageは今後、次のレイヤーを分離する。

1. **Geometry** — 道路の形
2. **KP Reference** — どのデータ源のKP目盛か
3. **Quality Gate** — 欠損・重複・不連続・版を監査
4. **Field Calibration** — 現場で実際に使う既知KPで補正
5. **Route Engine** — GPSからKPを推定

商品で重要なのは「公式データを使った」という看板ではなく、**どの基準のKPを表示しているかを追跡でき、必要なら現場基準で補正できること**。

## 現時点の禁止事項
- 現行R230/R453 legacyデータを「公式KP」と表現しない。
- 道路ネットワーク版100m座標を「実測KP」と表現しない。
- 出自不明データを全国テンプレートの正解教師にしない。
- 欠損や異座標重複を自動補間して黙って販売用Route Packageにしない。
- 現場照合前に行政提出精度や公式精度を保証しない。

## 次のゲート
1. 現行CSVとHG企画系KMLの完全一致比較を、取得経路が確保できれば実施。
2. 現場で既知の物理KP / 契約図書KPを複数点集め、各データ源と比較。
3. `Field Calibration`をRoute Package仕様に追加し、元データを消さず補正レイヤーとして保持。
4. Android実GPSで静止・走行時の総合誤差を測る。
5. 商用提供時は各データ源の利用・再配布条件を確認する。
